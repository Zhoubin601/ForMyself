# 分析与实施记录

## 2026-07-24 时间转盘与临近日程小组件字号

实施方向：

1. 时间选择器改为 24 小时时钟转盘：小时使用内外双环，选择小时后自动进入分钟转盘。
2. 分钟转盘提供 60 个真实刻度，触摸或拖动按 6° 映射到每一分钟，并提供 −1/+1 分钟微调。
3. 转盘、遮罩、确认按钮和动效继续使用应用内视觉，不恢复 Android 原生时间弹窗。
4. 临近日程 2×2 小组件标题由 11sp 放大到 13sp，时间由 8sp 放大到 10sp，事项标题由 9sp 放大到 11sp，计数和圆点同步小幅放大。

验证结果：

- 自动化测试 87/87 通过，新增分钟精度与小组件字号回归。
- Vite 生产构建、Capacitor Android 同步和 Android `assembleDebug` 构建通过。
- 新版已覆盖安装到真机 `V2403A` 并正常启动，未清除原应用数据。
- Debug APK：`output/20260724-time-dial-widget-v1-debug.apk`；SHA-256 为 `A718559B6564D8103F02A06C148F7185650FDA36E4E4C175D6240866CC0DF68B`。

## 2026-07-24 应用内反馈组件与侧边栏统一

实施方向：

1. 新增全局反馈服务和唯一宿主，统一处理提示、确认、输入、范围选择和轻提示，避免各页面复制弹窗样式。
2. 删除全部 77 处系统 `alert/confirm/prompt` 调用；删除类操作使用危险态确认，短成功反馈使用顶部胶囊轻提示。
3. 重复日程的 1/2/3 文本输入改为“仅本次 / 本次及以后 / 整个系列”可读选项面板。
4. 新增应用内日期和时间选择组件，替换全部 `type=date/time`；自动锁定时长改为应用内底部选择面板，并移除最后一个原生 `select`。
5. 系统生物识别、通知授权、文件选择和系统设置跳转仍保留原生界面，因为它们属于 Android 安全或系统能力边界，不应在 Web 层仿造。
6. 侧边栏采用带品牌标识的浅色玻璃面板，每个入口增加一致线性图标、辅助标签、明确选中态、关闭按钮和设备内隐私说明。
7. 根据模拟器截图复核，将侧边栏顶部临时字母标志替换为项目现有 App 图标 `public/icon.png`。

验证结果：

- 静态审计确认 `src/` 中不再出现 `alert`、`confirm`、`prompt`、原生 `select`、`type=date` 或 `type=time`。
- 自动化测试 84/84 通过，其中 3 项新增回归覆盖原生弹窗清零、原生选择器清零和侧边栏结构。
- Vite 生产构建、Capacitor Android 同步和 Android `assembleDebug` 构建通过。
- 最新 Debug APK：`output/20260724-ui-dialog-sidebar-v2-debug.apk`；v2 保留已有的非 5 分钟整数提醒时间，不会在打开时间选择器后被自动取整。

## 2026-07-24 模块设置拆分与锁屏美化

实施方向：

1. 设置 Store 增加 `settingsScope`、返回来源和模块设置打开/关闭动作，模块设置使用同一个可靠的设置页面，不复制持久化逻辑。
2. 仅省钱、体重、心情、日程和密码库显示独立设置入口；首页和月度报告不显示齿轮。
3. 省钱设置承载省钱提醒；体重设置承载身高、目标、变化阈值和提醒；心情设置承载标签和提醒；日程设置承载标签颜色；密码库设置承载分类管理。
4. 通用配置保留背景、桌面组件、安全、备份和公共 AI API；累计省钱看板文案实际服务于省钱模块，因此与省钱提醒一起放入省钱计划设置。
5. 侧边栏顶部使用 `public/icon.png` 真实 App 图标。
6. 锁屏升级为玻璃卡片和品牌光晕布局，加入真实 App 图标、显示/隐藏密码、按钮箭头、本机安全说明，并继续复用原主密码和指纹逻辑。
7. 日程页已有常驻搜索输入框，因此删除顶部重复的搜索图标，让菜单、年份和设置齿轮恢复三栏严格对称。

验证结果：

- 自动化测试 86/86 通过，新增回归确认首页不显示设置入口、省钱看板文案归属省钱计划，以及日程页没有重复的顶部搜索按钮。
- Vite 生产构建、Capacitor 同步和 Android `assembleDebug` 通过。
- `emulator-5554` 已覆盖安装并保留原数据；模拟器截图确认新锁屏和错误密码品牌化弹窗正常显示。
- 最新 Debug APK：`output/20260724-module-settings-lockscreen-v5-debug.apk`；SHA-256 为 `FD8D4B85C9CE09DF1CE70845984E8E8FAE7916C1A048DF914F90428F43A26CCC`。
- 最新模拟器启动截图：`output/20260724-v5-current-emulator.png`。

## 2026-07-24 vivo 风格日程系统

实施方向：
1. 新增独立日程领域服务与 Pinia Store，系列规则和单次 occurrence 状态分离保存。
2. 日程页提供月历/日程双视图、日期分组卡片、分类筛选、搜索、历史与完成筛选。
3. 编辑页采用 vivo 式顶部操作栏、横向分类和圆角分组表单，并保留 ForMyself 颜色与玻璃质感。
4. 通知使用独立 ID 区间和普通通知渠道，滚动生成近期发生项，避免与每日提醒冲突。
5. 完整备份升级为 v2，同时兼容导入 v1。
6. 第二个 AppWidget 读取预生成的未来 occurrence 快照，并在原生端按当前时间与未来 7 天再次筛选。
7. 日期持久化使用稳定公历日期，时间统一按北京时间处理，不依赖网络。
8. 根据真机反馈，最终版本移除农历和时区选项，统一按北京时间与公历工作。
9. 日程分类只内置“学习”，自定义标签统一在通用配置管理；编辑页不再临时创建标签。
10. 日程选择项使用自绘底部面板，并新增 1–365 天的自定义重复间隔。
11. 标签配置最终收敛为“名称 + 颜色”，移除图案选择及标签内图案；颜色由自绘 HSV 全色域面板、色相滑条和 HEX 输入共同控制，旧备份中的图案字段会安全忽略。
12. 日程进入、日程/月历切换、新建/编辑页和底部选择面板加入短时柔和过渡，并遵循系统“减少动态效果”设置。
13. 完整备份升级为 v2 并加入日程、occurrence 和标签；v1 导入自动补空日程。同时提供独立日程数据备份，备份类型选择改为自绘面板。
14. 根据真机反馈移除“闹钟提醒”按钮和闹钟渠道，旧数据/备份中的 `alarm` 自动迁移为普通通知；重新校准时只调度严格晚于当前时刻的任务，避免进入 App 后补发已经到点的同一通知。
15. 日程标签采用密码库分类的占用保护：普通系列或单次例外仍引用标签时显示使用数量且不提供删除入口，Store 层也会二次拒绝删除。
16. 默认日程时间线调整为此前 180 天至未来 365 天，按日期升序合并展示日程和待办；无事项的今天仍生成“今日暂无日程”卡片。
17. 顶部筛选仅保留“全部”，移除待办、已完成和历史的独立按钮；过期与已完成内容统一灰显并使用删除线表示历史状态。
18. 最终根据真机截图进一步删除独立“全部”按钮，仅保留分类筛选；年份、视图切换、搜索和分类区域固定，分类下方的日程/月历内容成为独立滚动窗口，打开日程默认定位窗口底部，向上查看旧内容。
19. 根据用户最新截图压缩日程顶部行：缩小非安全区上下留白、菜单/搜索图标和年份字号，同时保持年份严格居中与足够点击热区。

验证结果：

- Node 自动化测试 81/81 通过。
- Vite 生产构建、Capacitor Android 同步与 Android `assembleDebug` 构建通过。
- USB vivo V2403A 已完成前一轮日程列表和编辑页视觉检查；确认“历史”完整显示、仅保留“学习”、时区和农历已移除。
- 前一版 APK 已成功安装到 USB vivo V2403A，并由用户确认静息通知可以送达；本轮已构建修复重复通知、单一通知方式和标签占用保护的新 APK，尚待用户在手机上复测。
- 最新 APK 已覆盖安装到 `emulator-5554` 且保留备份数据；模拟器截图确认顶部缩短、默认显示最新日期、旧日程灰显/删除线以及分类下方独立滚动窗口均生效。
- 最终视觉截图为 `output/20260724-schedule-compact-header-v5.png`；Debug APK 为 `output/20260724-ForMyself-schedule-compact-header-v5-debug.apk`。

## 2026-07-24 Android 2×2 桌面信息卡

实施方向：

1. 使用 Android 原生 `AppWidgetProvider` 实现桌面占用 2×2 的信息卡小组件，保持 Capacitor Web 应用主体不变。
2. 信息卡展示今日心情、最新体重、省钱主目标进度和今日三项记录完成度；不在桌面展示密码库内容。
3. 三项信息行和顶部摘要使用独立显式 `PendingIntent` 和 `formyself://open/<模块>` 深链。
4. Vue 端同时监听热启动 `appUrlOpen` 和冷启动 `getLaunchUrl()`，统一切换现有 Pinia 页面状态。
5. 应用有主密码锁时只预选目标页面，仍需按原安全策略解锁后才能查看数据。
6. 原生 Provider 只读 Capacitor Preferences 中的心情、体重和省钱数据，解析失败时显示安全空状态；不读取密码库或 AI Key。
7. 通过轻量 Capacitor 插件在三类数据变化后主动刷新小组件，避免信息长期停留在旧值。
8. vivo 桌面会根据 `minWidth/minHeight` 推算占用格数；初版 140dp 被分配为 3×3，已调整为标准 110dp，以确保按 2×2 展示。
9. 根据用户进一步确认，2×2 指桌面占用面积而非内部四宫格；最终布局改为一体化信息卡。
10. 视觉采用 Android RemoteViews 可实现的“液态玻璃感”模拟：半透明品牌渐变、白色高光描边和透明胶囊；平台不支持桌面壁纸实时模糊或折射。

验证结果：

- Node 自动化测试 70/70 通过，覆盖 2×2 尺寸、信息字段、RemoteViews 安全布局和深链映射。
- Vite 生产构建、Capacitor Android 同步和 Android `assembleDebug` 均通过。
- Debug APK 已覆盖安装到 USB 真机 vivo V2403A，原有应用数据保留。
- vivo 桌面确认小组件实际占用 2×2，并展示今日完成度、今日心情、最新体重和省钱进度。
- 真机数据读取验证：截图时显示完成度 1/3、心情“一般”、体重 74.5 kg、省钱进度 8%。
- 三类数据变化后由 Capacitor 插件主动刷新；三行信息与顶部摘要分别可进入对应应用页面。
- 最终视觉为半透明玻璃卡片，含完整 ForMyself 标题、圆形应用图标、笑脸/体重秤/钱包图标和完成度胶囊。
- 最终效果图：`docs/phone-widget-round-icon.png`。

## 2026-07-18 密码库功能合并

实施方向：

1. 新增独立 Pinia 密码库 Store。
2. 密码记录在内存中以对象数组使用，写入 Preferences 前统一使用主密码 AES 加密。
3. 新存储 Key 使用 `my_password_vault_encrypted`，避免与旧明文数据混淆。
4. 首次加载时如发现旧 Key `my_password_manager_data`，解析后加密迁移；成功写入后删除旧明文 Key。
5. 修改主密码后使用内存中的密码记录，以新主密码重新加密写入。
6. 密码备份在设置页使用独立入口，兼容旧项目以主密码加密的数组格式。
7. UI 延续当前 ForMyself 的 Apple 风格和现有全局交互规范。

## 2026-07-18 心情多事件与标签升级

实施方向：

1. 心情记录继续使用扁平数组，但取消日期唯一约束，同一天允许多个独立事件。
2. 每条事件使用 `tags: string[]` 支持多标签，内置工作、学习、家庭、睡眠。
3. 旧记录或导入记录没有 `tags/tag` 时统一补为 `['学习']`。
4. 兼容曾使用单值 `tag` 的记录，导入后规范为 `tags` 并移除旧字段。
5. 自动补记记录增加 `autoFilled` 标记；用户首次补写该日事件时替换占位记录，避免重复统计。
6. 日历按天显示最新事件心情和事件数，列表按事件独立编辑、删除。
7. 月度情绪分布按事件次数计算，连续记录仍按唯一日期计算。

## 2026-07-18 密码库分类、收藏与二次生物识别

实施方向：

1. 密码记录新增 `category` 和 `favorite`，旧数据分别默认补为“未分类”和 `false`。
2. 兼容可能存在的旧别名 `group`、`isFavorite`，统一规范为新字段。
3. 收藏记录在列表中优先展示，并提供分类筛选、只看收藏与分类搜索。
4. 显示明文和复制密码分别执行一次独立生物识别；不可用、取消或失败均拒绝访问。
5. Web 环境不采用插件的模拟成功结果，必须是 Capacitor 原生平台才能发起密码访问授权。
6. 明文显示 30 秒后自动隐藏，页面进入后台或组件卸载时立即隐藏。

验证结果：

- Node 自动化测试 43/43 通过，覆盖旧结构迁移、别名兼容、收藏排序/切换，以及生物识别可用、不可用、取消和插件异常分支。
- Vite 生产构建通过；Android Gradle 构建通过（304 个任务）。
- Android 模拟器成功导入使用测试主密码加密的旧结构备份；两条记录均自动补为“未分类”和未收藏，随后验证了收藏筛选、自定义分类和分类筛选。
- Android 模拟器未录入生物识别时，“显示密码”和“复制密码”均被真实拒绝，密码保持隐藏。
- 模拟器的系统安全录入界面无法完成指纹样本注册，因此成功指纹弹窗分支由自动化桩测试覆盖；仍建议在已录入指纹或面容的真机上做最终交互确认。

## 2026-07-18 体重趋势增强

实施方向：

1. 健康设置增加身高、目标体重、体重变化提醒开关与阈值；兼容原来只保存 `targetWeight` 的设置。
2. BMI 根据最新有效体重和身高计算，仅作为趋势参考并明确不替代专业健康评估。
3. 周平均先合并同一天的多次称重为日均，再按周一至周日计算自然周平均，避免重复称重对周均造成额外权重。
4. 趋势图默认显示周均，同时允许切换每日记录；体重页摘要显示最新记录周均及与前一记录周的变化。
5. 新记录与上一条有效记录的变化达到用户阈值时显示温和页面提示；已有系统通知权限时同步发送本地通知，不使用医疗诊断措辞。
6. 保持旧体重备份数组格式不变；健康设置不混入旧记录，避免破坏既有导入。

验证结果：

- Node 自动化测试 50/50 通过，其中新增 7 项覆盖健康设置迁移、BMI、同日去重周均、无效日期、变化阈值和通知载荷。
- 真实体重备份 14 条在 Android 成功导入并生成 4 个周均分组。
- Android 验证身高/目标保存、BMI/目标差距、周均/每日切换、设置重启持久化均正常。
- Android 真实触发变化提醒，通知中心记录 ID `2201`，应用页面与系统通知同时生效。
- Vite 生产构建和 Capacitor 同步通过；Android `assembleDebug` 构建通过。

## 2026-07-18 完整数据备份

实施方向：

1. 在现有数据类型下拉框中新增“完整数据”，同时保留四类单项备份和原数组格式兼容性。
2. 完整备份使用带 `type/version/createdAt` 的版本化结构，包含省钱、体重、心情、密码库、心情追踪元数据和应用设置。
3. AI 服务配置（含 API Key）随完整备份迁移，但整个文件使用当前主密码 AES 加密；界面明确提示其敏感性。
4. 明确排除主密码和设备生物识别凭据，完整恢复不会改变当前认证方式。
5. 导入时先解密并校验文件类型、版本、日期及四个数据数组，再显示各类条数供用户确认；确认后覆盖全部数据与设置。
6. 写入失败时使用导入前的内存快照回滚；恢复完成后按恢复出的通知设置重新同步本地通知计划。
7. 省钱、体重、心情、密码库和设置 Store 均提供显式持久化恢复动作，避免只依赖异步监听器。

当前验证：

- Node 自动化测试 53/53 通过，其中新增 3 项覆盖完整快照字段、安全排除项、AES 往返与异常格式拒绝。
- Vite 生产构建、Capacitor Android 同步和 Android `assembleDebug` 构建通过（304 个任务）。
- Android 模拟器使用仅含虚构内容的完整备份完成真实恢复：确认框正确显示省钱、体重、心情、密码库各 1 条。
- 省钱计划金额、体重/BMI/目标、心情文字与自定义标签、密码库分类与收藏以及看板文案均恢复正确。
- 恢复前的一次性测试主密码仍可解锁；强制重启应用后密码库记录仍存在，验证了显式持久化。
- 完整导出成功生成 `ForMyself_Full_Backup_日期.json` 并打开 Android 系统分享面板。
- 测试完成后已删除模拟器下载目录中的虚构备份、清空测试应用数据并关闭模拟器；本地临时截图和生成脚本也已删除。

## 2026-07-18 应用图标更新与 v1.0 APK

实施方向：

1. 将 `newicon.png` 原图同步为 `assets/icon.png` 和 Android 项目图标源，不重新绘制或改变主体比例。
2. 生成 mdpi、hdpi、xhdpi、xxhdpi、xxxhdpi 五档普通、圆形、自适应前景和背景图标。
3. 自适应图标背景使用原图背景色 `#444E71`，移除旧的额外 inset，避免圆形裁切出现白边或主体过小。
4. 同步更新 PWA WebP 图标、网页 PNG favicon 入口和 manifest MIME 类型。
5. 提供 `scripts/generate-app-icons.mjs`，以后替换根目录 `newicon.png` 后可重复生成全部图标。
6. 保持 Android `versionName 1.0`；由于未配置生产密钥，交付 APK 使用 Android Debug 证书签名并在文件名中明确标记。

验证结果：

- Node 自动化测试 53/53 通过，Vite 生产构建和 Capacitor Android 同步通过。
- Android `clean assembleDebug` 构建通过（315 个任务）。
- APK 包名 `com.yubin.formyself`、版本 `1.0 (1)`、最低 API 24、目标 API 36 校验通过。
- APK Signature Scheme v2 验证通过；SHA-256 为 `32C99431BA194F5ABCB8905F56F702F6D7A0CDAD45200470B8339C97EB579F7B`。
- 模拟器卸载旧版本后全新安装最终 APK，Pixel Launcher 正确显示猫咪圆形图标，无白边且主体完整。
## 2026-07-22 每日通知可靠性修复

实施方向：

1. 为心情、体重、省钱和体重变化通知统一创建 Android 高重要性通知渠道，并在每条通知中显式指定 `channelId`。
2. 调度前先检查或申请显示权限，避免权限异常时先取消用户已有的系统提醒。
3. 调度后读取系统待处理通知并核对全部已启用 ID；发现缺失时明确报告失败，不再显示假成功。
4. 设置页显示通知权限、系统待处理提醒数量和精确闹钟状态，并提供一键立即发送的真机测试入口。
5. 每日提醒设置和系统校验成功后，立即发送“每日提醒已设置”通知，正文列出已开启项目和时间。
6. 增加通知服务的依赖注入测试，覆盖权限申请、渠道创建、调度校验、权限拒绝不误删、测试通知和设置成功通知。

验证结果：

- Node 自动化测试 60/60 通过，其中新增 7 项覆盖通知渠道、立即测试、设置成功确认、权限拒绝保护和系统待处理任务校验。
- Vite 生产构建和 Capacitor Android 同步通过。
- Android `assembleDebug` 构建通过（304 个任务），合并 Manifest 包含通知显示权限、开机恢复接收器和定时通知接收器。
- 生成 Debug APK `output/20260722-ForMyself-notification-fix-v1-debug.apk`，SHA-256 为 `2ED6CE693DA52F0066B78EFEC8C92EB600C71B0CF2B22D8D9C89737D44F9E5D9`。
- 在 `Pixel_6_Pro` Android 模拟器上全新安装 APK，真实完成通知权限授权；保存后通知栏显示“每日提醒已设置”，正文为“已开启 1 项提醒：心情 21:00”。
- 点击“发送一条测试通知”后，通知栏显示“ForMyself 通知测试”；系统记录 ID `2199`、渠道重要性 4。
- 将心情提醒改为 15:45 并把应用退到后台，Android 系统在非精确闹钟窗口内于约 15:47 发出“记录今天的心情”，系统记录 ID `2101`；随后自动生成次日 15:45 的调度项。
- 实测截图保存在 `output/20260722-notification-daily-trigger-emulator-v1.png`。模拟器未授予精确闹钟特殊权限，因此实际投递存在约 2 分钟延迟；不同手机的省电策略仍可能带来额外延迟。

## 2026-07-23 Android 精确每日提醒修复

实施方向：

1. 在 Android Manifest 声明 `SCHEDULE_EXACT_ALARM`，允许 Android 12+ 用户为 ForMyself 开启“闹钟和提醒”特殊权限。
2. 保存每日提醒后若未授权，明确提示可能延迟数分钟到一小时，并引导进入系统权限页面；设置页同时提供“开启准时提醒权限”按钮。
3. 用户拒绝特殊权限时保留原有非精确提醒作为降级方案，不把未授权误报为准时可用。
4. 从系统权限页返回后延迟 500ms 重新调度，解决权限刚生效但旧非精确任务尚未替换的时序问题。
5. 精确权限开启后重新校验系统待处理列表，继续发送“每日提醒已设置”确认通知。

验证结果：

- Node 自动化测试 62/62 通过，新增 2 项覆盖精确权限授权和无需重复打开设置的分支。
- Vite 生产构建、Capacitor Android 同步及 Android `assembleDebug` 构建均通过。
- 合并 Manifest 已包含 `android.permission.SCHEDULE_EXACT_ALARM`、`POST_NOTIFICATIONS` 和本地通知的定时/开机恢复接收器。
- `Pixel_6_Pro` 模拟器真实完成通知权限授权、进入“Alarms & reminders”系统页并开启特殊权限。
- 16:20:22 的心情提醒在应用退到后台后准点触发；AlarmManager 记录为 `window=0`、`exactAllowReason=permission`。
- 触发后自动安排的次日 16:20:22 任务仍为 `window=0` 精确闹钟，修复了首次触发后次日任务退化为一小时窗口的问题。
- 实测截图：`output/20260723-notification-exact-trigger-emulator-v2.png`。
- 交付 APK：`output/20260723-ForMyself-notification-exact-fix-v2-debug.apk`；SHA-256 为 `0F3A334573E7B231733A4990F1BFD9713DA8AE0FB82A8585ADBB219911308B9B`。

## 2026-07-23 应用关闭后的每日唤醒修复

实施方向：

1. 修补 Capacitor Local Notifications 8.2.1 的原生每日重排逻辑：从持久化通知读取 `allowWhileIdle`。
2. 精确权限可用时，次日继续使用 `setExactAndAllowWhileIdle(AlarmManager.RTC_WAKEUP, ...)`；无精确权限时保留 `setAndAllowWhileIdle(AlarmManager.RTC_WAKEUP, ...)` 降级路径。
3. 新增幂等的 `postinstall` 脚本 `scripts/patch-capacitor-local-notifications.mjs`，确保重新安装依赖后原生修补不会丢失；插件源码版本不匹配时构建前明确失败，避免静默回归。
4. 设置页说明划掉最近任务、进程被系统回收、手机重启、系统“强制停止”之间的差异，并提醒部分品牌手机允许自启动和后台运行。

验证结果：

- Node 自动化测试 63/63 通过；新增原生源码回归测试，要求重排路径同时包含 `RTC_WAKEUP`、`setExactAndAllowWhileIdle` 和 `setAndAllowWhileIdle`。
- Vite 生产构建、Capacitor Android 同步和 Android `assembleDebug` 均通过。
- 进程结束验证：16:55:21 前 ForMyself 进程不存在，系统仍准点发出 ID `2101` 的“记录今天的心情”通知。
- 重排验证：触发后的次日 16:55:21 任务保持 `RTC_WAKEUP`、`window=0`、`exactAllowReason=permission`，不再退化为普通 `RTC`。
- 重启验证：模拟器重启后不启动 ForMyself，开机接收器恢复精确闹钟并准点发送通知。
- 强制停止验证：Android 将包标为 `stopped=true` 并删除闹钟；此平台限制无法由本地代码绕过，重新打开应用后现有启动同步逻辑会恢复提醒。
- 交付 APK：`output/20260723-ForMyself-notification-background-fix-v3-debug.apk`，SHA-256 为 `7885BB0FF076C07DA44A18FFC3BFAA4ECD7B3EE71DA59A3A2B21C007E05D292A`。
- 实测截图：`output/20260723-notification-closed-app-trigger-emulator-v3.png`。

## 2026-07-23 心情标签与密码分类管理 v2

实施结果：

1. 通用配置新增“内容标签与分类”区域；心情日记仅展示可删除的自定义标签，内置标签固定保留。
2. 删除心情自定义标签时，同时从所有历史心情记录中清理；若某条记录因此没有任何标签，则安全回退为“学习”。
3. 密码库分类改为独立加密保存，支持在通用配置新增和删除，并纳入完整数据备份与恢复。
4. “未分类”作为系统保留分类不可删除；任何仍被密码记录引用的分类显示“已使用 N 条”，不提供删除入口，Store 层也会再次拒绝删除。
5. 密码新增/编辑器的分类字段由 Android WebView 支持不稳定的 `datalist` 改为原生 `select`，编辑现有记录时可正常打开、选择并保存。
6. 删除分类后，如果密码库当前筛选器仍指向该分类，会自动回退到“全部分类”。

验证结果：

- Node 自动化测试 66/66 通过，覆盖标签同步删除、无标签回退、分类增删、占用保护、系统分类保护和完整备份恢复。
- Vite 生产构建通过。
- Pixel 6 Pro Android 模拟器完成真实交互：新增测试分类；新增密码记录；编辑记录并从原生下拉框选择测试分类；验证使用中显示“已使用 1 条”且无删除入口；删除记录后分类恢复可删并成功删除。
- 模拟器新增带自定义标签的心情记录，在通用配置删除标签后，标签列表变为空，历史记录中只保留内置“学习”标签。
- 交付 APK：`output/20260723-ForMyself-tag-category-management-v2-debug.apk`；大小 9,951,995 字节；SHA-256 为 `D175B3B6DB369C7673CB5B76072E3D2BED4D662AD5BC1D8CD5A83AB9E4706D31`。
- 最终 v2 APK 已重新安装到 `emulator-5554`，安装成功并正常启动 `com.yubin.formyself/.MainActivity`；包版本保持 `1.0 (1)`。

## 2026-07-23 首页看板视觉拓展

实施结果：

1. 顶部改为随早晨、下午和夜间变化色调的问候主卡，集中展示日期、今日记录数量和每日陪伴文案。
2. 心情、体重和存钱入口合并为“今日记录”卡片组，按当天数据显示已记录状态并可直接进入对应模块。
3. 省钱目标改为横向进度卡，展示主目标名称、完成比例、已存与目标金额；无目标时提供明确入口。
4. 体重卡增加最近七条有效记录的迷你趋势线；心情卡保留七天表情并显示记录天数摘要。
5. 新增最近动态，将真实心情、体重和省钱记录按时间聚合，点击可进入对应模块；自动补记的心情占位不进入动态。
6. 新增醒目的月度报告入口；密码库数据未放到首页，避免敏感信息暴露。
7. 过期 AI 首页缓存不会再与当前问候时间冲突；非当天文案会回退为基于今日记录数量的中性提示。

验证结果：

- Node 自动化测试 66/66 通过，Vite 生产构建、Capacitor Android 同步和 Android `assembleDebug` 均通过。
- Pixel 6 Pro Android 模拟器完成顶部、下半页、滚动密度和月报入口跳转检查；未发现文字溢出或点击区域异常。
- 效果图：`output/20260723-home-dashboard-redesign-v1-top.png`、`output/20260723-home-dashboard-redesign-v1-bottom.png`。

## 2026-07-23 密码库分类下拉框视觉优化

实施结果：
1. 分类筛选器改为应用内自绘卡片：增加分类图标、双层文字、旋转箭头、选中高亮和勾选反馈。
2. 密码新增/编辑弹窗的分类字段使用同一套视觉语言，并将当前分类置顶，打开后可立即确认当前选择。
3. 编辑弹窗内的选项面板改为随表单自然展开，避免绝对定位菜单被弹窗滚动区域裁切或压住底部操作按钮。
4. 点击选项、点击组件外部、关闭弹窗时都会正确收起菜单；原有分类筛选与保存行为保持不变。

验证结果：
- Node 自动化测试 66/66 通过，Vite 生产构建通过，`git diff --check` 通过。
- Capacitor Android 同步和 Android `assembleDebug` 构建通过。
- 自定义选择器初版已在 Pixel 6 Pro 模拟器检查关闭态、筛选展开态和编辑展开态；根据截图发现编辑菜单底部拥挤后又完成了布局修正。
- 最终布局修正版因当前执行环境无法再次调用电脑上的 Android SDK，尚未重新安装到模拟器复测；该状态已明确记录，未视为已完成的真机/模拟器验证。
- 预览图：`output/20260723-password-vault-dropdown-redesign-v1-filter.png`、`output/20260723-password-vault-dropdown-redesign-v1-editor.png`。
- 交付 APK：`output/20260723-ForMyself-password-dropdown-redesign-v1-debug.apk`，大小 9,951,995 字节，SHA-256 为 `65B3B721ADD5691D0166BEEE608E068905C6C1FD436AF702F36B5563B8CA1277`。

## 2026-07-26 Galaxy 微动效视觉增强

实施方向：

1. 保持现有蓝色、白色和液态玻璃视觉，不直接复制 Galaxy 的深色霓虹配色。
2. 全局增加两层低透明度径向柔光，以 `transform` 和 `opacity` 做慢速漂移；自定义壁纸启用时不叠加。
3. 主按钮吸收 Galaxy 移动渐变与扫光思路，降低饱和度、速度和阴影强度，保证文字对比度。
4. 首页增加一次性的分层入场、主卡柔光漂移、状态点呼吸、卡片触控扫光和月报横幅渐变。
5. 月报卡片使用错峰入场，统计条由左向右展开；日程新增按钮只做轻微呼吸与按压旋转。
6. 所有持续动画保持低频，并通过 `prefers-reduced-motion` 全局禁用，避免眩晕和不必要耗电。
7. 不增加第三方运行时依赖，不修改业务数据、备份结构或 Android 原生代码。

验证结果：

- 新增 3 项动效静态回归测试，完整 Node 测试 93/93 通过。
- Vite 生产构建通过，Capacitor Android 同步通过，Android `assembleDebug` 构建通过。
- `git diff --check` 通过；未发现空白错误。
- 生成 Debug APK `output/20260726-ForMyself-galaxy-motion-v1-debug.apk`，大小 11,113,079 字节。
- APK SHA-256 为 `762EED511A1EA7F940020C2E33A05F7CABEDC96A33705345A318389D90A5A687`。
- 当前环境没有可用浏览器或已连接的 ADB 设备，因此尚未做真机动态观感与帧率复核；该项需要用户安装后确认。

## 2026-07-26 全应用灵动治愈 UI 与主题系统

实施方向：

1. 新增独立主题规范化与颜色派生服务，四套治愈预设加自定义主色，自动保证主按钮前景对比度。
2. 主题设置使用独立 Preferences 键持久化，并作为可选字段纳入完整备份 v2；旧备份缺少主题时回退云朵蓝。
3. 设置页复用现有应用内 HSV 调色逻辑，不新增 WebView 原生颜色选择器。
4. 将全局品牌色、柔光、卡片边框与阴影迁移到语义 CSS 变量；状态色、心情色和用户日程标签色保持独立。
5. 压缩顶部导航和首页纵向空间，消除透明导航穿透，统一中文栏目名、圆角、阴影和主要图标容器。
6. 保留现有侧栏、业务流程、自定义壁纸和减少动态效果支持，不加入底部导航或深色模式。
7. 使用用户指定的加密完整备份在 Android 模拟器做真实恢复与页面巡检；密码只在运行时输入，不落盘。

实施与验证结果：

1. 新增云朵蓝、蜜桃粉、薄荷绿、晚霞紫四套预设及应用内 HSV 自定义色盘；主色会派生浅表面、边框、阴影和满足 4.5:1 白字对比度的强强调色。
2. `settingsStore` 已持久化主题并把 `settings.theme` 纳入完整备份 v2；旧备份缺少主题时回退云朵蓝，完整备份往返测试通过。
3. 顶部栏改为高不透明实体玻璃层，首页主卡收紧至约 240px，主要模块间距统一为 16px；首页英文眉题已替换为中文，月报入口改为主题浅色卡。
4. 日程、日期/时间选择器、反馈弹层、体重图表、心情与省钱等页面的品牌强调色已迁移到主题变量；心情色、成功/警告/危险色和用户标签色保持独立。
5. Node 自动化测试 99/99 通过；Vite 生产构建、Capacitor Android 同步和 Android `assembleDebug` 均通过。
6. Pixel 6 Pro 模拟器完成加密备份恢复：识别省钱 22 项、体重 16 条、心情 37 条、密码 37 项、日程 4 项；恢复后逐页检查首页、日程、心情、体重、省钱、密码库、设置、月报、锁屏和确认弹层。
7. 模拟器验证四套预设实时切换、自定义极亮 `#EAE9F0`、极暗 `#111112`、自定义背景共存、重启持久化和旧备份导入；未发现崩溃、顶部穿透、明显文字溢出或低对比度主按钮。
8. 测试完成后已删除模拟器中的备份副本、过程截图并清空应用测试数据；桌面原始备份未修改。包含账号标识的密码库检查截图未保留。
9. 初版交付 APK：`output/20260726-ForMyself-healing-theme-v1-debug.apk`。
10. vivo V2403A 真机复核发现顶部栏仍有轻微内容透出，汉堡图标在圆角按钮内视觉偏右；已将顶部栏改为实体背景，并用固定 38px 点击区、Flex 居中和对称占位修正图标位置。
11. 修正版已通过 99/99 自动化测试、Vite 构建、Capacitor 同步和 Android `assembleDebug`，并以保留数据方式覆盖安装到 V2403A；真机截图确认菜单图标水平、垂直居中，顶部栏无内容穿透。
12. 最终修正版：`output/20260726-ForMyself-healing-theme-v2-debug.apk`；SHA-256 为 `3BE96F2244E26EC0F0569A3E1DBCC602583955EF2AAED2A7447A514083F2E8CB`。

## 2026-07-26 女朋友式 AI 多日陪伴提示词

实施方向：

1. 将完整女朋友式人设与心情回音、首页简报、通知三个场景约束拆分，方便未来聊天模块复用人设而取消短文案限制。
2. 以目标日期为基准纳入近 30 天全部真实记录，保留完整文字，排除自动补记、无效数据和当前事件重复项；同日心情按日期分组。
3. 人设按语境自然切换“宝宝”和“哥哥”，低落时先接住情绪、开心时充分庆祝；只有多日证据充分时才描述趋势。
4. 心情回音目标 80–160 字、硬上限 200 字；首页 80–120 字、硬上限 120 字；通知硬上限 35 字。
5. 首页指纹改为完整规范化上下文并包含日期，仅成功生成后提交，保证首次、跨日、历史变化和失败重试行为正确。

实施与验证结果：

1. 新增共享陪伴提示词服务，包含完整人设、称呼规则、六段情绪链、多日证据规则、安全边界、三类示例和三个场景构建器。
2. 心情新增与编辑均使用本次记录加近 30 天历史；界面“闺蜜”文案已替换为专属女孩式表达。
3. 首页和个性化通知已接入同一历史构建逻辑；月报、存钱节点鼓励和 API 连接测试保持原定位。
4. 新增真实字符硬截断与标点优先收尾，模型偶尔超长时不会撑破对应展示场景。
5. 自动化测试 107/107 通过，Vite 生产构建通过；未修改 `raw/`，未生成新的正式交付文件。

## 2026-07-26 “温馨小家”AI 女朋友聊天模块

实施方向：

1. 新增版本化 `ChatSnapshot`、消息/记忆规范化服务、AES 文件存储和响应式聊天 Store；主文件替换使用临时文件与上一版备份，读取失败时禁止覆盖原密文。
2. 复用完整女朋友人设并注入动态名字；聊天请求组合长期记忆、近 30 天心情/体重/省钱、前后 30 天日程、临时欢迎语和聊天历史。
3. 扩展 AI 引擎以支持 OpenAI 兼容 SSE、跨分片解析、停止生成、非流式回退、上下文超限分类及 R1/GLM 人格首条注入，同时保持 `askAI(prompt)` 兼容。
4. 新增全高聊天页面、侧栏和首页入口；支持欢迎语、最近 100 条初始渲染、逐批加载、复制、删除、重试、发送/停止、流式光标和键盘安全区。
5. 每轮完整回复后仅提取本轮长期记忆，使用严格 JSON 校验、敏感内容过滤、按 key 去重 upsert；设置页支持名字和记忆手动管理及三种清理操作。
6. 完整备份升级到 v3 并纳入聊天回滚；新增 `formyself-chat-backup` v1 独立加密备份，支持覆盖和合并。

实施与验证结果：

1. 自动化测试 130/130 通过，覆盖聊天排序去重、分页、三种清理、AES 往返/错误密码/损坏保护/临时恢复、欢迎语生命周期、SSE 跨块与 `[DONE]`、中止、非流式及中途失败回退、上下文降级、R1 注入、记忆提取容错和备份兼容。
2. Vite 生产构建、Capacitor Android 同步和 Android `assembleDebug` 均成功。
3. Pixel 6 Pro 模拟器完成入口、本地兜底欢迎语、全高布局、键盘安全区、设置、手动记忆和备份选择器巡检；加密聊天主文件与上一版备份文件均实际生成。
4. 最终 APK 已在模拟器覆盖安装成功；验收结束后清空应用测试数据并关闭模拟器。
5. 测试环境没有配置可调用的用户 AI API Key，因此未在模拟器中向真实服务商发送聊天；流式协议、网络回退和上下文超限降级由自动化测试覆盖。
6. 最终 Debug APK 位于 `android/app/build/outputs/apk/debug/app-debug.apk`，大小 11,113,079 字节，SHA-256 为 `CFA9CFD0CAFA140520F7D018891B8AC7568E4C134FFC4F39B3706C2552E33CBA`。

### 2026-07-26 真人私聊语气修正

真机反馈表明，聊天直接复用完整陪伴人设中的六段情绪链，会让模型对普通消息也习惯性复述、分析、关联多日记录、鼓励并陪伴收尾，形成标准 AI 情绪价值文案。

修正结果：

1. 增加聊天专属最高优先级规则，明确覆盖“每轮完整情绪链”；短消息默认只回一到四句，复杂心事仍可自由展开，不设置字符上限。
2. 降低称呼、追问、建议和多日记忆的出现频率；生活数据默认作为后台印象，一轮最多自然呼应一件直接相关的旧事。
3. 明确禁止复述、升华、心理分析和“听起来、我能感受到、从记录来看”等典型 AI 套话。
4. 允许口语、省略句、停顿、调皮、轻微不同意和不完整的小反应，同时禁止控制、贬低和编造现实线下经历。
5. 欢迎语改为看到男朋友上线后的短私聊，不再总结数据或使用通用客服欢迎语；聊天温度调整为 0.92，欢迎语为 0.94。
6. 新增 3 项真人感回归测试；完整自动化测试 133/133、Vite 构建、Capacitor Android 同步和 Android `assembleDebug` 全部通过。
7. 修正版已保留数据覆盖安装到 vivo V2403A 并成功启动；APK SHA-256 为 `B42384CF8DCE6E9FD7CDE27E3C55224B0B3220B76D6AEC16C0BC8CE5156AB215`。

### 2026-07-26 聊天底栏与设置窄屏布局修正

1. 将聊天页从百分比高度网格改为与日程页一致的固定动态视口和纵向 Flex：顶部导航、身份栏、底部输入区不可收缩，只有消息列表占用剩余空间并独立滚动。
2. `chat-content-area` 使用 `flex: 1 1 0` 和 `height: 0` 消除 Android WebView 中长消息撑开父容器的问题；聊天外层增加 `overflow: hidden`。
3. 增加消息底部锚点，进入页面时先定位一次，下一动画帧再次校准；发送、流式回复和完成回复仍持续保持底部定位。
4. 设置页补齐文本容器 `min-width: 0`、按钮全宽规则和窄屏专属字号/间距；长期记忆输入框与添加按钮在窄屏改为上下排列。
5. 新增 2 项布局回归测试；完整自动化测试 135/135、Vite 构建、Capacitor Android 同步和 Android `assembleDebug` 全部通过。
6. 修正版已保留数据覆盖安装到 vivo V2403A 并成功启动；APK SHA-256 为 `9759824ABF24AD529CF89652F81EFB6AC6BE5BB5D31F463D95DBA49A2D583F31`。
7. 追加修正名字字段：`label.chat-name-field` 显式改为块级元素，并在输入框与全宽保存按钮之间增加 14px 间距，避免原行内元素的垂直 margin 不生效。
8. 最终间距修正版已保留数据覆盖安装到 vivo V2403A 并成功启动；APK SHA-256 为 `B3CA18599E0C041D97F4B22CB4A66D8C076F7568BB1B68678C9485476965C05B`。

### 2026-07-26 AI 连续小气泡回复

1. 新增回复拆分器：优先按模型自然分段拆气泡，过长段落再按完整句子和软标点拆分，最终气泡不保留空行或可见分隔符。
2. 流式生成阶段直接把已收到内容渲染为多条连续气泡，只有最后一条显示流式光标；停止生成时把已有分段全部保存为中止消息。
3. 一轮完成后批量保存各气泡，但只使用整轮内容执行一次长期记忆提取，避免分段导致重复记忆。
4. 向模型发送历史时重新合并连续同角色气泡，完整语义和历史内容不损失，也减少服务商对连续 assistant role 的兼容问题。
5. 提示词要求多层意思以空行表达下一条消息；小说式动作旁白允许保留，但会与紧接着的对白合并在同一个气泡。
6. 新增 5 项拆分、历史合并、流式展示、单轮记忆和旁白合并测试；完整自动化测试 140/140、Vite 构建、Capacitor 同步和 Android `assembleDebug` 全部通过。
7. 修正版已保留数据覆盖安装到 vivo V2403A 并成功启动；APK SHA-256 为 `5F95D83082ED12AC80BAC622436CF07C8C938214CCF517B1870C76C05362BAF7`。

### 2026-07-26 聊天首次进入直接显示底部

1. 根因是消息列表全局设置了 `scroll-behavior: smooth`，Android WebView 会将首次 `scrollTop` 定位也动画化。
2. 消息列表改为 `scroll-behavior: auto`；进入页面期间先隐藏列表，完成底部定位和下一帧校准后再直接显示，避免顶部内容闪现。
3. 发送、流式回复和完成回复继续自动贴住底部，但不再播放从顶部滑下来的动画。

### 2026-07-26 自定义头像、引用回复与记忆分页

1. 聊天档案新增安全栅格头像字段；设置页支持 JPG、PNG、WebP 上传，浏览器端居中裁成 512×512 JPEG，并限制原文件不超过 5MB。头像显示在身份栏和 AI 消息旁，随加密聊天文件、独立备份和完整备份保存。
2. 消息新增 `replyTo` 快照；长按真实消息气泡会出现引用、复制、删除操作面板。选择引用后，输入区上方显示原消息摘要，发送后引用关系永久保留；点击气泡内引用可定位原消息，原消息删除后仍保留引用时的文字快照。
3. 向模型发送历史时，将引用内容转换成明确但不可见于 UI 的语境说明，帮助女朋友理解“这个、那句、就这样”等指代；提示词要求直接接话，禁止复述引用功能说明。
4. 独立备份合并继续保留本机当前女朋友档案，因此不会被导入备份中的名字或头像意外替换；覆盖导入和完整恢复则完整恢复头像与引用关系。
5. 长期记忆按更新时间排序，每页固定显示 3 条；新增后回到第一页，删除、清空或重置后自动校正页码。
6. 新增头像裁剪、头像/引用规范化、备份往返、模型引用上下文和界面资源回归测试；完整自动化测试 144/144、Vite 生产构建通过。
7. Capacitor Android 同步与 `assembleDebug` 构建通过；新版已保留数据覆盖安装到 vivo V2403A 并成功启动，进程 PID 为 14618。APK 大小 11,113,079 字节，SHA-256 为 `DA7E8AAF08F40AEF2E75015C2DB24B4BAE08CB7ACD9EFCD3EF086E9E315BEAAB`。
