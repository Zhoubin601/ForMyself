# 省钱计划 Android 模拟器验收

日期：2026-09-09。

## 测试对象与隔离方式

- 用户要求改用模拟器，继续省钱计划页面验收。
- 使用 Pixel_6_Pro AVD，安装 `output/20260908-ForMyself-savings-drag-v1-debug.apk`。
- 安装包 SHA-256：`9E5E77B3481FE5B11F56DA501832528846152F6763A28B4D4F8A8BDF60844EE3`。对应源码保存在 `Temp` 分支的 `f7c6aec`；当前工作区为 `main`，本轮未切换分支或修改功能源码。
- 回归使用独立 ephemeral Android 用户 10 及 25 个虚构目标；初始化密码随机生成，仅在测试进程内存中使用，不写入项目文件。
- 测试通过实际模拟器 WebView 输入事件操作卡片，通过 Android 按键处理返回，通过 force-stop 验证进程重建后的数据。

## 已通过

| 场景 | 验证结果 |
| --- | --- |
| 页面结构 | 固定标题显示，25 个目标全部渲染，411 CSS px 视口无页面横向溢出 |
| 长按拖动 | 手柄长按可提起卡片，拖至边缘触发自动滚动，松手保存新顺序 |
| 撤销 | 撤销恢复原顺序；再次拖动能够重新保存 |
| 强制关闭重开 | force-stop 后重启、密码解锁，再进入省钱页，顺序与保存结果一致 |
| Android 返回键 | 拖动中返回取消本次调整，顺序不变；明细弹层返回正常关闭 |
| 编辑与达成 | 从更多菜单编辑目标名称，存入剩余金额后进入已达成分类 |
| 删除 | 已达成目标经确认删除，记录数量正确减少 |
| 应用锁屏 | 锁屏后使用同一临时密码重新解锁成功 |

结果由临时回归脚本保存在 `Test_data/savings-emulator-result.json`，仅包含布尔结果和虚构记录数量，无私人内容或密码。

## 环境问题与限制

- 首次运行时模拟器系统遮挡导致 WebView 隐藏；唤醒并关闭遮挡后重新执行，交互回归全部通过。
- 初始截图抓到了页面过渡中的空白帧，没有作为视觉通过证据。补拍期间模拟器意外断开，随后重新启动；本轮不交付该无效截图。
- 用户此前提供的 `ForMyself_Full_Backup_2026-09-08.json` 位于微信临时路径，该文件现已不存在；在原临时目录及项目资料目录未找到同名副本。本轮没有执行真实备份导入，也没有使用其他备份替代。
- 真实备份恢复仍需重新提供原文件。此次模拟器结果不等同于物理手机验收，也未调用用户的 AI API。

## 清理

退出测试用户 10 后已由系统清理。补拍创建的临时用户 11 也在模拟器重启后清理；复查仅剩原 Owner 用户 0。调试转发已移除。没有清除原 Owner 的业务数据，没有改写原始资料。
